/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['assets.aceternity.com','utfs.io','images.unsplash.com','cdn.sanity.io'],
    },
  };
  
  export default nextConfig;
  